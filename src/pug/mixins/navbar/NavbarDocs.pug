mixin NavbarDocs 
  ul#component-nav.components-nav.list-unstyled
    li 
      h5.text-uppercase Docs
    each value, index in sitemap[5].pages 
      li.mb-1
        a(href=`${CWD}${value.path}.html` class=pathName === value.path ? 'text-black' : '')
          =value.name